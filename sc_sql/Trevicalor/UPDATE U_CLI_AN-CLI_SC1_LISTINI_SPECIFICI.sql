
/*
UPDATE U_CLI_AN SET CLI_SC1 = 20 WHERE CLI_CODCLI IN (
	SELECT CCODCLI FROM IMPIANTI_TT WHERE ID_PMKEY IN (SELECT ID_PMKEY FROM IMPIANTI_TE WHERE CCODLIS = '-28'))
*/

SELECT CLI_SC1,* FROM U_CLI_AN WHERE CLI_CODCLI IN (
	SELECT CCODCLI FROM IMPIANTI_TT WHERE ID_PMKEY IN (SELECT ID_PMKEY FROM IMPIANTI_TE WHERE CCODLIS = '-28'))
ORDER BY CLI_CODCLI