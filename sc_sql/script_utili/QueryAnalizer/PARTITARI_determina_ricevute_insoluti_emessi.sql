--- FATTURE EMESSE
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE ( PAR_TIPFAT='VEN' OR PAR_TIPFAT='CF1')
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/15' 
	AND PAR_CODCLI='1010200001768') AS PC

--- RICEVUTE EMESSE
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE PAR_TIPFAT='CRB'
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2011/01/01' AND '2012/04/11' 
	AND PAR_CODCLI='1010200001768') AS PC

--- INSOLUTI RICEVUTI
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE PAR_TIPFAT='CIN'
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/11' 
	AND PAR_CODCLI='1010200001768') AS PC

--- INSOLUTI RICEVUTI
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE PAR_TIPFAT='CIN'
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2011/01/01' AND '2011/12/31' 
	) AS PC

--- FATTURE EMESSE
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE (PAR_TIPFAT='CF1' OR PAR_TIPFAT='VEN'  )
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/15' 
	) AS PC

--- NOTE CREDITO
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE (PAR_TIPFAT='CN1')
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/15' 
	) AS PC



--DETTAGLIO PARTITA 
SELECT PAR_DTSCAD,PAR_DTFAT,PAR_NUMFAT,PAR_NPART,* FROM PART_CLI 
WHERE PAR_TIPFAT='CIN'
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/11' 
	AND PAR_CODCLI='1010200001768'

--- BONIFICI INCASSATI
SELECT SUM(PAR_FATT) FROM (
SELECT PAR_DTSCAD,PAR_DTFAT,PAR_NUMFAT,PAR_NPART,* FROM PART_CLI 
WHERE PAR_TIPFAT='CBO'
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/11' 
	AND PAR_CODCLI='1010200001768') AS PC

SELECT PAR_TMOV,PAR_TIPFAT,PAR_FATT,PAR_DTSCAD,* FROM PART_CLI 
WHERE PAR_NPART=121762
	AND PAR_NPROG<>100
	AND PAR_DTSCAD='2012/03/10'
	AND PAR_CODCLI='1010200001768'





/*****************************************************
TOTALE TUTTI I CLIENTI
*****************************************************/

--- FATTURE EMESSE
SELECT SUM(PAR_FATT) FROM (
SELECT * FROM PART_CLI 
WHERE ( PAR_TIPFAT='VEN' OR PAR_TIPFAT='CF1' )
	AND PAR_NPROG<>100
	AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/15') AS PC

-- FATTURE
SELECT SUM(PAR_FATT) FROM (
   		SELECT 	COALESCE(CL.CLI_RAGSOC,PA.PAR_RAGSOC) AS CLI_RAGSOC,
   				COALESCE(CL.CLI_CAP,'') AS CLI_CAP,
   				COALESCE(CL.CLI_INDIR,'') AS CLI_INDIR,
   				COALESCE(CL.CLI_LOCALI,'') AS CLI_LOCALI,
   				COALESCE(CL.CLI_AGE,'') AS CLI_AGE,
   				COALESCE(CL.CLI_AGE,'') AS AGENTE,
				PA.PAR_TIPFAT  AS TIPOOPE ,
				COALESCE(SUBSTRING(PARTOP.LIBERA,2,1),'')  AS FLAG_NATURA ,
				COALESCE(SUBSTRING(PARTOP.LIBERA,10,1),'')  AS FLAG_RIBA_TOP ,
				COALESCE(SUBSTRING(PAR.LIBERA,10,1),'')  AS FLAG_RIBA,
   				PA.* 
   		FROM PART_CLI AS PA 
   		LEFT OUTER JOIN U_CLI_AN AS CL 	
   		ON SUBSTRING(PA.PAR_CODCLI,6,LEN(PA.PAR_CODCLI))=CL.CLI_CODCLI 
   		LEFT OUTER JOIN PARA AS PAR
   		ON PAR.CODICE='CPA'+PA.PAR_CODPAG 
   		LEFT OUTER JOIN PARA AS PARTOP
   		ON PARTOP.CODICE='TOP'+PA.PAR_TIPFAT  
   		WHERE PAR_CODSOC = '01'
WHERE FLAG_NATURA='S'  AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/15'



-- RIBA
SELECT SUM(PAR_FATT) FROM (
   		SELECT 	PA.PAR_TIPFAT  AS TIPOOPE ,
				COALESCE(SUBSTRING(PARTOP.LIBERA,2,1),'')  AS FLAG_NATURA ,
				COALESCE(SUBSTRING(PARTOP.LIBERA,10,1),'')  AS FLAG_RIBA_TOP ,
				COALESCE(SUBSTRING(PAR.LIBERA,10,1),'')  AS FLAG_RIBA,
   				PA.* 
   		FROM PART_CLI AS PA 
   		LEFT OUTER JOIN PARA AS PAR
   		ON PAR.CODICE='CPA'+PA.PAR_CODPAG 
   		LEFT OUTER JOIN PARA AS PARTOP
   		ON PARTOP.CODICE='TOP'+PA.PAR_TIPFAT  
   		WHERE PAR_NPROG <> 100 ) AS PC  
WHERE FLAG_RIBA_TOP ='S'  AND PAR_DTFAT BETWEEN '2012/01/01' AND '2012/04/15'