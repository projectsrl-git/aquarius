

IF M.ORD_CAUS = PUB_INTE		&& INTERVENTI TECNICI
	M.ORD_CAUS	= "LIC"			&& LISTA CONSEGNA
ELSE
	IF M.ORD_CAUS = "LIC"		&& LISTA CONSEGNA
		M.ORD_CAUS	= PUB_INTE	&& INTERVENTI TECNICI
	ENDIF
ENDIF


IF M.ORD_CAUS = "LIC"		&& LISTA CONSEGNA
	IF M.ORD_CODNAZ="ITA"
	  	 M.ORD_RIFERI="LISTA CONSEGNA NR. "+M.ORD_NUMORD + "   DEL "+ M.ORD_DATORD
	ELSE
	     M.ORD_RIFERI="DELIVERY LIST NR. "+M.ORD_NUMORD + "   VOM "+ M.ORD_DATORD  
	ENDIF

ELSE
	IF M.ORD_CODNAZ="ITA"
	  	 M.ORD_RIFERI="INTERVENTO NR. "+M.ORD_NUMORD + "   DEL "+ M.ORD_DATORD
	ELSE
	     M.ORD_RIFERI="INTERVENTION NR. "+M.ORD_NUMORD + "   VOM "+ M.ORD_DATORD  
	ENDIF
ENDIF

KEYBOARD(REPLICATE("{ENTER}",1))