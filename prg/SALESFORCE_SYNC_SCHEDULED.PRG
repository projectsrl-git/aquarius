****************************************
* SALESFORCE_SYNC_SCHEDULED
****************************************

LOCAL _SAVE_SEL,_PDF_SUBDIR_,_FILE_DA_ALLEGARE,_PDF_DESCRIZIONE_,_PDF_SELECT_ ,_PDF_REPORT_ ,_PROGRESSIVO_PROGRAMMA,_ALLEGATI

_SAVE_SEL = SELECT()
_PROGRESSIVO_PROGRAMMA = SYS(2015)

_ALLEGATI=""
_FILE_DA_ALLEGARE=""
_PDF_DESCRIZIONE_=""
_PDF_SELECT_ =""
_PDF_REPORT_ =""
_PDF_SUBDIR_ = LEGGE_CONFIG("AQ_SCHEDULER_CONFIG.CFG","PATH_EMAIL")	&& RICERCA DIRECTORY E SOTTODIRECTORY CONTENENTE IL FILE PDF DA CREARE

RELEASE V_TITOLO
PUBLIC V_TITOLO

V_TITOLO = "Sincronizzazione dati con Salesforce"

WAIT WINDOWS "ESEGUE PROGRAMMA SALESFORCE_SYNC_SCHEDULED" TIMEOUT 3

SET PROCEDURE TO PRG\APPLILIB ADDITIVE
SET PROCEDURE TO PRG\XLSLIB	ADDITIVE
SET PROCEDURE TO PRG\REPLIB	ADDITIVE
*SET PROCEDURE TO PRG\WINLIB ADDITIVE
SET PROCEDURE TO PRG\PROJLIB ADDITIVE
SET PROCEDURE TO PRG\SALESFORCE_REST_API_LIB ADDITIVE
SET PROCEDURE TO PRG\JSON ADDITIVE

RELEASE PUB_ANNO,PUB_CARORF,PUB_DECQTA,PUB_SKART,PUB_FORMAR,PUB_BOLLA,PUB_MAGPDMVERS,PUB_SCPPDMSPED,PUB_SPASCA,PUB_SCAPPT,PUB_INTXLS,PUB_SFLOGIN ,PUB_SFLOGOUT,PUB_SFUSER,PUB_SFPASS,PUB_SFCKEY,PUB_SFCSECRET, ;
	PUB_MASTRO,PUB_SOTTOG,PUB_LCL,PUB_CONTO,PUB_CC,PUB_CTRCLOR,PUB_NAZI,PUB_AGE,PUB_CONTOSIMILCEE,PUB_GRUPPOCF,PUB_AGEOPE
PUBLIC PUB_ANNO,PUB_CARORF,PUB_DECQTA,PUB_SKART,PUB_FORMAR,PUB_BOLLA,PUB_MAGPDMVERS,PUB_SCPPDMSPED,PUB_SPASCA,PUB_SCAPPT,PUB_INTXLS,PUB_SFLOGIN ,PUB_SFLOGOUT,PUB_SFUSER,PUB_SFPASS,PUB_SFCKEY,PUB_SFCSECRET, ;
	PUB_MASTRO,PUB_SOTTOG,PUB_LCL,PUB_CONTO,PUB_CC,PUB_CTRCLOR,PUB_NAZI,PUB_AGE,PUB_CONTOSIMILCEE,PUB_GRUPPOCF,PUB_AGEOPE
PUB_ANNO = SUBSTR(DTOC(DATE()),7,4)
PUB_CARORF = ""
PUB_DECQTA = 0
PUB_SKART = .F.
PUB_FORMAR = ""
PUB_BOLLA = ""
PUB_MAGPDMVERS = ""
PUB_SCPPDMSPED = ""
PUB_SPASCA = ""
PUB_SCAPPT = ""
PUB_INTXLS = .F.
PUB_SFUSER = ""
PUB_SFPASS = ""
PUB_SFCKEY = ""
PUB_SFCSECRET = ""
PUB_SFLOGIN = ""
PUB_SFLOGOUT = ""
**=> ERASMO - INIZIO 29/04/2020
PUB_MASTRO = 0
PUB_SOTTOG = 0
PUB_LCL = 0
PUB_CONTO = 0
PUB_CC = ""
**=> ERASMO - FINE 29/04/2020
PUB_CTRCLOR = .F.  && ERASMO - 05/05/2021
**=> ERASMO - INIZIO 14/04/2025
PUB_NAZI = ""
PUB_AGE = ""
PUB_CONTOSIMILCEE = .F.
PUB_GRUPPOCF = 8
**=> ERASMO - FINE 14/04/2025
PUB_AGEOPE = ""  && ERASMO - 18/04/2025


Csql = "SELECT * FROM RES_OPER WHERE CODICE = '" + PUB_CODOPE + "'"
IF !ExecCommand(cSql,'CU_RES_OPER')
	return(.f.)
ENDIF
SELECT CU_RES_OPER
GO TOP
IF !EOF()
	PUB_NOMOPE = DESCRI
	PUB_AGEOPE = RES_AGEOPE  && ERASMO - 18/04/2025
ENDIF

Csql = "SELECT * FROM U_AZI_AN"
IF !ExecCommand(cSql,'CU_AZI_AN')
	return(.f.)
ENDIF
SELECT CU_AZI_AN
GO TOP
IF !EOF()
	PUB_CARORF = AZI_CARORF
	PUB_DECQTA = AZI_DECQTA
	PUB_SKART = AZI_SKART
	PUB_FORMAR = AZI_FORMAR
	PUB_BOLLA = AZI_BOLLA
	**=> ERASMO - INIZIO 29/04/2020
	PUB_MASTRO = AZI_MASTRO   && GRUPPO CONTABILITA GENERALE       + 1
	PUB_SOTTOG = AZI_SOTTOG   && SOTTOGRUPPO CONTABILITA GENERALE  + 1
	PUB_LCL    = AZI_CONTO    && CONTO CONTABILE
	PUB_CONTO  = AZI_CONTO
	IF PUB_MASTRO <> 0
	   PUB_MASTRO = PUB_MASTRO + 1
	ELSE
	   PUB_MASTRO = 4
	ENDIF
	IF PUB_SOTTOG <> 0
	   PUB_SOTTOG = PUB_SOTTOG + 1
	ELSE
	   PUB_SOTTOG = 6
	ENDIF
	IF PUB_LCL = 0					&& valore già impostato e valido anche per i clienti/fornitori
	   PUB_LCL = (14 - PUB_SOTTOG)
	ENDIF
	**=> ERASMO - INIZIO 29/04/2020
	**=> ERASMO - INIZIO 14/04/2025
	PUB_NAZI = AZI_NAZI
	PUB_AGE = AZI_AGE
	**=> ERASMO - FINE 14/04/2025
ENDIF

Csql = "SELECT * FROM U_AZI_PA"
IF !ExecCommand(cSql,'CU_AZI_PA')
	return(.f.)
ENDIF
SELECT CU_AZI_PA
GO TOP
IF !EOF()
	PUB_MAGPDMVERS = AZI_MAGPDMVERS
	PUB_SPASCA = AZI_SPASCA
	PUB_SCAPPT = AZI_SCAPPT
ENDIF

Csql = "SELECT * FROM U_AZI_PB"
IF !ExecCommand(cSql,'CU_AZI_PB')
	return(.f.)
ENDIF
SELECT CU_AZI_PB
GO TOP
IF !EOF()
	PUB_SCPPDMSPED = AZI_SCPPDMSPED
	PUB_INTXLS = AZI_INTXLS	
	PUB_SFLOGIN = AZI_SFLOGIN
	PUB_SFLOGOUT = AZI_SFLOGOUT
	PUB_SFUSER = AZI_SFUSER
	PUB_SFPASS = AZI_SFPASS
	PUB_SFCKEY = AZI_SFCKEY
	PUB_SFCSECRET = AZI_SFCSECRET
	PUB_CTRCLOR = AZI_CTRCLOR  && ERASMO - 05/05/2021
ENDIF

**=> ERASMO - INIZIO 14/04/2025
Csql = "SELECT * FROM U_AZI_PC"
IF !ExecCommand(cSql,'CU_AZI_PC')
	return(.f.)
ENDIF
SELECT CU_AZI_PC
GO TOP
IF !EOF()
	PUB_CONTOSIMILCEE = AZI_CONTOSIMILCEE
ENDIF
**=> ERASMO - FINE 14/04/2025

_ERRORE = .F.
_LOG_GROUP_CODSYS = SYS(2015)
_CORPO2 = ""

*** PRE-CONTROLLO ELABORAZIONI PENDENTI
*cSql = "SELECT * FROM LOG_SFSYNC WHERE ESITO = '' AND SUBSTRING(MESSAGGIO,1,12) = 'ELABORAZIONE'"
**=> ERASMO - INIZIO 16/12/2024
cSql = "SELECT * FROM LOG_SFSYNC WHERE ESITO = '' AND SUBSTRING(MESSAGGIO,1,12) = 'ELABORAZIONE' " + ;
	"AND OGGETTO IN ('CLIENTI','ARTICOLI','ORDINI_TESTATE','ORDINI','DDT_TESTATE','DDT','ASSETS','FATTURE_TESTATE','FATTURE')"
**=> ERASMO - FINE 16/12/2024	
IF !ExecCommand(cSql,"TMP_CTRL_SFSYNC")
	return(.F.)
ENDIF
SELECT TMP_CTRL_SFSYNC
GO TOP
IF !EOF()
	_ERRORE = .T.
	_CORPO2 = "Sono in corso una o più elaborazioni pendenti per le opzioni selezionate, schedulazione annullata"
ELSE

	_OK_SYNC_CLIENTI = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_CLIENTI WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_CLIENTI
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'CLIENTI'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Clienti con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale clienti inseriti da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun cliente inserito da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale clienti aggiornati da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun cliente aggiornato da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECINS_FROM_SF > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale clienti inseriti da Salesforce: " + ALLTRIM(STR(NRECINS_FROM_SF))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun cliente inserito da Salesforce"
*!*			ENDIF
*!*			
*!*			*IF NRECAGG_FROM_SF > 0
*!*			*	_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9)) + "Totale clienti aggiornati da Salesforce: " + ALLTRIM(STR(NRECAGG_FROM_SF))
*!*			*ELSE
*!*			*	_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9)) + "Nessun cliente aggiornato da Salesforce"
*!*			*ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	
	_OK_SYNC_ARTICOLI = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_ARTICOLI WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_ARTICOLI
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'ARTICOLI'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Articoli con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale articoli inseriti da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun articolo inserito da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale articoli aggiornati da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun articolo aggiornato da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	
	_OK_SYNC_ORDINI_TESTATE = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_ORDINI_TESTATE WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_ORDINI_TESTATE
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'ORDINI_TESTATE'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Testate Ordini con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale testate ordini inserite da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessuna testata ordine inserita da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale testate ordini aggiornate da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessuna testata ordine aggiornata da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	
	_OK_SYNC_ORDINI = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_ORDINI WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_ORDINI
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'ORDINI'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Dettagli Ordini con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale dettagli ordini inseriti da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun dettaglio ordine inserito da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale dettagli ordini aggiornati da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun dettaglio ordine aggiornato da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	
	**=> ERASMO - INIZIO 02/02/2024
	_OK_SYNC_DDT_TESTATE = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_DDT_TESTATE WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_DDT_TESTATE
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'DDT_TESTATE'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Testate DDT con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale testate DDT inserite da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessuna testata DDT inserita da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale testate DDT aggiornate da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessuna testata DDT aggiornata da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	
	_OK_SYNC_DDT = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_DDT WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_DDT
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'DDT'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Dettagli DDT con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale dettagli DDT inseriti da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun dettaglio DDT inserito da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale dettagli DDT aggiornati da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun dettaglio DDT aggiornato da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	**=> ERASMO - FINE 02/02/2024
	
	_OK_SYNC_ASSETS = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_ASSETS WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_ASSETS
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'ASSETS'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Assets con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale assets inseriti da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun asset inserito da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale assets aggiornati da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun asset aggiornato da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF
	
	_OK_SYNC_FATTURE_TESTATE = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_FATTURE_TESTATE WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_FATTURE_TESTATE
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'FATTURE_TESTATE'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Testate Fatture con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale testate fatture inserite da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessuna testata fattura inserita da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale testate fatture aggiornate da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessuna testata fattura aggiornata da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF

	_OK_SYNC_FATTURE = .F.
	DO PRG\SALESFORCE_SINCRONIZZA_FATTURE WITH .T.,_LOG_GROUP_CODSYS
	IF !_OK_SYNC_FATTURE
		_ERRORE = .T.
	ENDIF
	Csql = "SELECT * FROM LOG_SFSYNC WHERE CODSYS = '" + _LOG_GROUP_CODSYS + "' AND OGGETTO = 'FATTURE'"
	IF !ExecCommand(cSql,'TMP_LOG_SFSYNC')
		return(.f.)
	ENDIF
	SELECT TMP_LOG_SFSYNC
	GO TOP
	IF !EOF()
		_CORPO2 = ALLTRIM(_CORPO2) + IIF(!EMPTY(ALLTRIM(_CORPO2)),REPLICATE(CHR(13)+CHR(10),2),"") + "Sincronizzazione Dettagli Fatture con Salesforce (esito " + ALLTRIM(ESITO) + "):"
*!*			IF NRECINS_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale dettagli fatture inseriti da Aquarius: " + ALLTRIM(STR(NRECINS_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun dettaglio fattura inserito da Aquarius"
*!*			ENDIF
*!*			
*!*			IF NRECAGG_FROM_AQ > 0
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Totale dettagli fatture aggiornati da Aquarius: " + ALLTRIM(STR(NRECAGG_FROM_AQ))
*!*			ELSE
*!*				_CORPO2 = _CORPO2 + CHR(13)+CHR(10)+CHR(9) + "Nessun dettaglio fattura aggiornato da Aquarius"
*!*			ENDIF
		_CORPO2 = ALLTRIM(_CORPO2) + CHR(13)+CHR(10) + ALLTRIM(MESSAGGIO)  && ERASMO - 04/04/2025
	ENDIF

ENDIF

*!*	RELEASE PROCEDURE PRG\APPLILIB 
*!*	RELEASE PROCEDURE PRG\XLSLIB
*!*	RELEASE PROCEDURE PRG\REPLIB
*!*	*RELEASE PROCEDURE PRG\WINLIB
*!*	RELEASE PROCEDURE PRG\PROJLIB
*!*	RELEASE PROCEDURE PRG\SALESFORCE_REST_API_LIB
*!*	RELEASE PROCEDURE PRG\JSON


*!*	_PDF_REPORT_ 		=ALLTRIM(CERCAREPO("XXX","ORDINI_NON_EVASI","ORDINI_NON_EVASI.FRX"))
*!*	_PDF_DESCRIZIONE_	= V_TITOLO+_PROGRESSIVO_PROGRAMMA 
*!*	_PDF_SELECT_ 		= "CU_ORDINATO"

*!*	IF !EMPTY(_PDF_SUBDIR_ )
*!*		=CREA_FILE_PDF(_PDF_SELECT_,_PDF_REPORT_,_PDF_DESCRIZIONE_,_PDF_SUBDIR_ )
*!*	ELSE
*!*		=CREA_FILE_PDF(_PDF_SELECT_,_PDF_REPORT_,_PDF_DESCRIZIONE_)
*!*	ENDIF

*!*	_FILE_DA_ALLEGARE = ALLTRIM(_PDF_DESCRIZIONE_)+".PDF"

*!*	_ALLEGATI = _ALLEGATI + IIF(EMPTY(_ALLEGATI),"",";")+_FILE_DA_ALLEGARE 

_ALLEGATI = ""
DO PRG\INVIA_MAIL_SCHEDULATE WITH _ERRORE,_ALLEGATI,_ID_EMAIL_DA_INVIARE,_CORPO2


SELECT(_SAVE_SEL )
	
